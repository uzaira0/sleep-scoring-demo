repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.7
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format

  - repo: local
    hooks:
      - id: basedpyright
        name: basedpyright (warnings only)
        entry: bash -c 'basedpyright || true'
        language: system
        types: [python]
        pass_filenames: false
        verbose: true
